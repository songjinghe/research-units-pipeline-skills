# Bad Examples

Avoid these failure modes:

- reader-facing prose paragraphs instead of bullets
- fixed domain neighborhoods in `Related Work` when the taxonomy does not imply them
- Python-only heuristics that encode agent / tool / RAG / security framing
- placeholder bullets like `TODO`, `TBD`, `Discuss limitations`, or scaffold instructions
- repeated vague bullets that do not change with the subsection title
