# Status

## Current workstream
- repo-wide reference-first normalization, now focused on reader-facing survey-writer cleanup under the new contracts, with `subsection-writer` coverage/voice now the tightest bottleneck

## Current checkpoint
- `C5` (front matter is improving, but the live blocker is still `subsection-writer` quality/coverage on a few survey H3s)

## Next units
- `U164` keep pushing `subsection-writer` upstream: remove the remaining weak snippet carryover, stop low-value fallback prose, and recover enough H3 thickness / citation diversity on the current weak H3s (`4.1`, `4.2`, `5.1`, `5.3`) to pass `writer-selfloop` again without downstream patching
- `U166` finish de-templating chapter-opening glue so H2 lead blocks and subsection handoffs no longer reintroduce outline narration into sections `1/2/3`
- `U163` finish smoothing front matter now that the worst planner-talk is gone, especially the remaining axis-list / organization-forward phrasing in Abstract / Introduction / Related Work
- `U165` tighten voice gates only after the new target prose for `U163/U164` is stable enough to encode as the stricter baseline
- `U190` rerun the clean embodied-AI retest to a real PASS again only after `U163/U164/U166/U165` are stable; compile-only previews do not count as closure
- `U150` keep shrinking helper-invented survey defaults once the reader-facing writer bottlenecks above are stable
- `U170` recalibrate `audit_skills` so strict Anthropic-style gating remains usable after the reference-first migrations
- `U180` fix the remaining workspace-contract drift in `agent-survey-corpus`

## Run log
- 2026-03-08: rebuilt the ideation pipeline around `REPORT.md` + `APPENDIX.md` + `REPORT.json`.
- 2026-03-09: completed reference-first foundations and the first migration batch for writer/planner skills.
- 2026-03-10: closed the repo-wide validation baseline (`validate_repo.py` clean; Phase 4 complete).
- 2026-03-11: shifted the remaining refactor scope to pipeline-contract normalization, helper-policy cleanup, audit trust, and representative survey validation.
- 2026-03-11: rewrote `z_refactor` to latest-standard form so it now tracks only current standards and current open work.
- 2026-03-11: added `ANTHROPIC_SKILLS_DESIGN_PRINCIPLES.md` to capture the Anthropic skills design model, especially the process and packaging principles encoded by `skill-creator`.
- 2026-03-11: completed an embodied-AI latex-survey smoke run to end-state PASS, exposing that the current survey pipeline is strongest in late-stage audit/repair but still too H3-first early in structure formation.
- 2026-03-11: added `SURVEY_PIPELINE_STRUCTURE_DESIGN.md` to define the target survey workflow as retrieval-informed, section-first, and loop-driven rather than query-only or purely linear.
- 2026-03-11: added `SURVEY_PIPELINE_DESIGN_REVIEW.md` to test the new design against the current survey contracts, with explicit migration risks and minimum contract changes.
- 2026-03-11: added `SURVEY_PIPELINE_CONTRACT_CROSSWALK.md` to map the current survey contract to the target section-first contract and to close artifact-lineage ambiguity before metadata-first implementation.
- 2026-03-11: ran a full 6-agent re-review of `z_refactor` and tightened the docs around parse boundary, precedence, variant rules, stable-H3 cutover, section-first gate, and execution-order dependencies.
- 2026-03-11: started the first implementation batch for `U130/U145/U150/U152`: parser and validator now accept frontmatter-first stage contracts, survey and idea pipelines now expose machine-readable defaults/stages, and kickoff/quality-gate/idea-brief paths now consume contract-driven defaults instead of only helper fallbacks.
- 2026-03-11: started `U140`: `arxiv-survey-latex` now resolves through `variant_of` / `variant_overrides`, `artifact-contract-auditor` now reads resolved pipeline specs, and variant load failures now fail contract audit instead of silently degrading to a false PASS.
- 2026-03-11: closed `U140`: variant overrides now support list-patch operators (`__append__` / `__remove__` / `__replace__`), `arxiv-survey-latex` has been reduced to a true latex/pdf delta, and schema docs now describe the real variant merge contract.
- 2026-03-11: started the ideation skill cleanup that sits next to `U145/U152`: `idea-brief` now has local references/assets, and its method/query-bucket/rubric contract is being moved out of Python toward a reference-first package shape.
- 2026-03-11: closed `U130/U145`: survey and ideation pipelines now expose machine-readable defaults/stages under the frontmatter-first contract, runtime loading validates variant top-level boundaries, and `validate_repo.py --strict --no-check-docs --no-check-quality` stays green.
- 2026-03-11: closed `U153`: `idea-brief` is now a reference-first package with local references/assets, stricter asset validation, explicit `report_top_n` replay, and less script-owned kickoff policy.
- 2026-03-11: started `U154`: ideation C4/C5 runners and QA now fail closed on missing/invalid pipeline specs, consume canonical declared `queries.md` overrides instead of legacy query alias keys, and read `idea-brief` required sections from the asset contract rather than a duplicated QA list.
- 2026-03-11: tightened `U154` further: `idea-brainstorm` screening/shortlist thresholds and diversity targets now start moving into `quality_contract`, and ideation runtime/QA consume those contract fields instead of keeping the numbers only in code.
- 2026-03-11: tightened `U154` further again: ideation score weights now come from pipeline contract, shortlist diversity axes now come from contract, and shortlist rationale sentence templates now live in a dedicated skill asset instead of only in Python.
- 2026-03-11: advanced `U150`: survey quality-gate readers now only honor canonical query keys for survey defaults instead of old alias keys such as `writing_profile`, `core_set_size`, or `per_h3`.
- 2026-03-11: continued `U154`: score weights and lead-set diversity axes now live in `idea-brainstorm` `quality_contract` and are consumed by the ideation runtime instead of being fixed inside the screener/shortlist selectors.
- 2026-03-11: started executable `U155` insertion: survey contracts and UNITS now include a real section-first C2 producer layer (`chapter-skeleton`, `section-bindings`, `section-briefs`) before stable H3 mapping; `outline-builder` now supports section-first inputs and `outline-refiner` now records section-first cutover keys in `outline_state.jsonl`.
- 2026-03-11: validated the new section-first C2 chain in a `/tmp` smoke workspace using embodied-AI survey inputs: `chapter-skeleton -> section-bindings -> section-briefs -> outline-builder -> section-mapper -> outline-refiner` completed and produced stable `outline_state.jsonl` with `structure_phase: decomposed` and `h3_status: stable`.
- 2026-03-11: extended the section-first smoke one layer deeper: after the new C2 chain, `subsection-briefs` and `chapter-briefs` also ran successfully on the derived stable outline, confirming the inserted section-first layer can already feed the current H3 planning stack.
- 2026-03-11: tightened downstream section-first gating: `subsection-briefs`, `writer-context-pack`, and `quality_gate` now read `outline_state.jsonl` and require `structure_phase: decomposed` plus `h3_status: stable` before accepting H3-first downstream artifacts in section-first runs.
- 2026-03-11: connected the new section-first skills to strict execution gating: `quality_gate.check_unit_outputs()` now validates `chapter-skeleton`, `section-bindings`, and `section-briefs`, and `outline-refiner` now keeps section-binding BLOCKED / REROUTE cases at unstable cutover state instead of silently treating the H3 layer as ready.
- 2026-03-11: extended the same cutover requirement into C4 inputs: `evidence-binder` and `evidence-draft` now also require section-first `outline_state` to report `decomposed + stable` before H3 evidence artifacts can regenerate.
- 2026-03-11: fixed a remaining `U155` contract bug in the section-first chain: `section-bindings` now emits structured `status` values, `hold_or_merge` now materializes as `REROUTE` instead of a false `PASS`, `quality_gate` now checks JSONL/report consistency for PASS/BLOCKED/REROUTE, and repo-local reroute-vs-pass smokes confirm `binding_reroute` no longer leaks downstream as stable H3 readiness.
- 2026-03-11: made the reroute path more operational: `outline-refiner` now prefers structured section-binding state over markdown-only inference when computing cutover, and executor-side block messages now surface `reroute_reason` so a failed section-first run points back to the chapter that triggered reroute.
- 2026-03-11: completed a real embodied-AI `arxiv-survey-latex` workspace rerun under the current repo state at [workspaces/latex-survey/embodied-ai-smoke-20260311-r2](/home/rjs/Workspace/codebase/research-units-pipeline-skills/workspaces/latex-survey/embodied-ai-smoke-20260311-r2): the run ends `IDLE` with `AUDIT_REPORT.md`, `LATEX_BUILD_REPORT.md`, and `CONTRACT_REPORT.md` all PASS, and the previously missing section-first artifacts (`chapter_skeleton`, `section_bindings`, `section_binding_report`, `section_briefs`, `REROUTE_STATE`) are materialized; because the workspace originated from a pre-insertion units file, treat it as artifact-level latex validation rather than the sole proof that the current executable template is clean.
- 2026-03-11: improved late C5 output quality for the same embodied-AI workspace by de-templating subsection opener templates, stabilizing deterministic template selection, splitting oversized appendix tables during LaTeX scaffold generation, and mapping Unicode `π` in generated TeX; the rebuilt PDF no longer reports `Float too large` or missing-character warnings and now has only one residual underfull hbox warning.
- 2026-03-11: the real workspace rerun exposed a remaining contract/gate mismatch: `section-logic-polisher` can still report `FAIL` while the unit is allowed to complete because `quality_gate` currently treats that report as diagnostic-only. This is now tracked explicitly as follow-up `U156` instead of being left implicit.
- 2026-03-11: completed a current-template embodied-AI `arxiv-survey` rerun at [workspaces/arxiv-survey/embodied-ai-smoke-20260311-r1](/home/rjs/Workspace/codebase/research-units-pipeline-skills/workspaces/arxiv-survey/embodied-ai-smoke-20260311-r1); after adding deterministic H3-opener stem deduplication to `subsection-writer`, the workspace now ends `IDLE` with both `AUDIT_REPORT.md` and `CONTRACT_REPORT.md` PASS, giving the survey-side proof that the normalized section-first contract executes end-to-end without silent fallback.
- 2026-03-11: closed `U156`: `quality_gate` now blocks when `SECTION_LOGIC_REPORT.md` is not PASS, so `section-logic-polisher` no longer silently degrades to a diagnostic-only step; regression checks confirm old FAIL reports are blocked while both embodied-AI smoke workspaces still pass.
- 2026-03-11: closed `U152/U154`: ideation C4/C5 runtime now ignores numeric target prose in `IDEA_BRIEF.md`, rejects out-of-range canonical query overrides instead of silently clamping them, validates score weights / diversity axes as contract fields, and QA/scripts now consume resolved contract values without shadow `12/24/5/3` fallbacks.
- 2026-03-11: deleted redundant z_refactor planning/checklist docs after folding their active points into the kept blueprint and execution artifacts.
- 2026-03-12: reran the embodied-AI `arxiv-survey` smoke workspace to end-state PASS at [workspaces/arxiv-survey/embodied-ai-smoke-20260311-r1](/home/rjs/Workspace/codebase/research-units-pipeline-skills/workspaces/arxiv-survey/embodied-ai-smoke-20260311-r1); `AUDIT_REPORT.md`, `CONTRACT_REPORT.md`, and `LATEX_BUILD_REPORT.md` all pass, `main.pdf` rebuilds successfully (49 pages), and the late C5 chain now blocks/repairs repeated closer stems instead of silently passing them.
- 2026-03-12: this PASS is not yet a clean proof that `U100` is one-shot safe. Direct `subsection-writer` regeneration still regressed H3 quality under the current writer-context packs, so the successful tail rerun depended on restoring and freezing the stronger H3 bodies before merge/citation/audit/latex. The main remaining skill-level risk is therefore still the raw `subsection-writer` generation path, not the late C5 tail chain.
- 2026-03-12: the final closer-stem cleanup that moved the embodied-AI smoke draft from near-pass to final `AUDIT_REPORT.md` PASS was applied at the merged-draft layer before the last audit/latex rerun. Treat the current draft/PDF as the reference artifact, but do not mistake that for a proof that the underlying `sections/` layer and raw `U100` generator are already aligned without manual intervention.
- 2026-03-12: hardened the late survey writing chain after the embodied-AI rerun exposed two concrete regressions: `subsection-writer` / `draft-polisher` / `transition-weaver` were still emitting fixed opener/bridge/closer macros, and `citation-injector` was incorrectly treating appendix content as part of the last H3 block. The tail chain is now de-templated enough to remove the repeated closer family (`The safest synthesis ...`, `That conclusion remains ...`, `... decision rule is therefore ...`) and the injector stops before new `##` headings so appendix tables are no longer polluted by citation top-up prose.
- 2026-03-12: reran the embodied-AI `arxiv-survey` workspace again after those fixes to a clean, internally consistent end-state at [workspaces/arxiv-survey/embodied-ai-smoke-20260311-r1](/home/rjs/Workspace/codebase/research-units-pipeline-skills/workspaces/arxiv-survey/embodied-ai-smoke-20260311-r1): `AUDIT_REPORT.md` PASS with 203 unique citations, `CONTRACT_REPORT.md` PASS, `LATEX_BUILD_REPORT.md` SUCCESS with no float warnings, and `main.pdf` rebuilt successfully at 57 pages. The workspace has also been cleaned of transient section backups and LaTeX auxiliary files.
- 2026-03-12: pushed the fix point back upstream into the writer skills themselves: `subsection-writer` now chooses among topic-derived opener candidates instead of a single stock opener path, `opener-variator` / `style-harmonizer` are marker-only rather than blind regex editors, and `transition-weaver` stays non-injective by default so section openings and chapter leads carry coherence instead of canned bridge prose.
- 2026-03-12: verified that the embodied-AI workspace can now pass the hard `U100 -> U1005 -> ... -> U150 -> U130` chain without restoring frozen H3 bodies. Current end-state artifacts remain PASS at [workspaces/arxiv-survey/embodied-ai-smoke-20260311-r1](/home/rjs/Workspace/codebase/research-units-pipeline-skills/workspaces/arxiv-survey/embodied-ai-smoke-20260311-r1), though front-matter paragraphing and final PDF warning count still need comparison against the previous best artifact before claiming clear quality superiority.
- 2026-03-12: a deeper trace of the front-matter path shows the next upstream bottleneck is now `front-matter-writer`: its asset bank still stores whole reader-facing paragraphs rather than a thinner hook/constraint layer, so Introduction/Related Work voice remains more template-led than the H3 body chain.
- 2026-03-12: a deeper trace of the middle-artifact path shows another upstream leak: `evidence_drafts` / `writer_context_packs` still preserve survey-style source excerpts such as `In this survey ...`, and `subsection-writer` can surface those snippets almost verbatim in early generated prose unless snippet sanitation is tightened before writing.
- 2026-03-12: reran the embodied-AI `arxiv-survey` workspace again to a clean end-state PASS at [workspaces/arxiv-survey/embodied-ai-smoke-20260311-r1](/home/rjs/Workspace/codebase/research-units-pipeline-skills/workspaces/arxiv-survey/embodied-ai-smoke-20260311-r1): `AUDIT_REPORT.md` PASS, `CONTRACT_REPORT.md` PASS, `LATEX_BUILD_REPORT.md` PASS, and `latex/main.pdf` rebuilt successfully at 57 pages with only 3 overfull + 3 underfull hbox warnings.
- 2026-03-12: this final PASS supersedes the previous optimistic note about raw `U100` one-shot safety. The successful tail rerun used the stronger restored H3 bodies plus `h3_bodies.freeze.ok`, so the remaining unresolved risk is still the direct `subsection-writer` generation path rather than the late C5 merge/citation/audit/latex tail.
- 2026-03-12: confirmed and fixed three concrete tail-chain defects during the same smoke run: `chapter-lead-writer` no longer emits rigid three-paragraph planning-style leads, `citation-injector` now counts only actually inserted keys and can emit multiple short citation-grounding sentences per H3, and the appendix-table corruption / `Float too large for page` failure disappeared after rerunning `U101 -> U150` on a clean appendix block.
- 2026-03-12: a full final-to-source trace plus clean C5 retest clarified the new high-priority backlog order. The biggest remaining problems are no longer repeated H3 closer stems; they are (1) front matter still being generated from whole-paragraph prose banks, (2) source-paper narration leaking through evidence packs into writer outputs, (3) writer-side gating still under-enforcing some front-matter / H3 quality constraints, and (4) `latex-scaffold` leaking `__LATEX_PH_*__` placeholder residue into TeX even when compilation succeeds.
- 2026-03-12: tightened the immediate front-matter path accordingly: `front-matter-writer` templates no longer contain the worst deictic `for this run`-style phrasing, and `writer-selfloop` now treats thin `Introduction` / `Related Work` as hard failures. The clean C5 retest therefore now blocks earlier and more honestly at front matter instead of drifting to a late false-positive tail PASS.
- 2026-03-12: closed `U158`: `front-matter-writer` now renders `Introduction` / `Related Work` from a hook-bank job graph under `front_matter_contract.json` rather than a whole-paragraph prose bank; the clean C5 retest no longer blocks at the front-matter gate.
- 2026-03-12: closed `U159` at the writer-pack boundary: strengthened snippet sanitation in `evidence-draft` / `writer-context-pack` so writer context packs no longer carry the previously observed source-paper self-narration triggers into downstream drafting, even though raw `evidence_drafts.jsonl` still preserves some diagnostic source phrasing.
- 2026-03-12: closed `U160`: `writer-selfloop` now treats pipeline voice and materially weak H3s (`sections_h3_sparse_citations`, `sections_h3_too_few_paragraphs`, `sections_h3_too_short`) as hard failures; the clean C5 retest now blocks honestly on raw subsection-body quality instead of front matter.
- 2026-03-12: inserted `U162` because the main remaining one-shot blocker moved to `subsection-writer`; after the follow-up body-generator refactors, the clean embodied-AI retest now gets all H3 files to 10 paragraphs and the local citation floor no longer blocks `writer-selfloop`.
- 2026-03-12: closed `U162`: `subsection-writer` now uses denser subsection bodies, broader in-scope citation coverage, and less snippet-shaped clause templates; the clean C5 retest no longer blocks at `writer-selfloop`.
- 2026-03-12: closed `U161`: `latex-scaffold` now fails fast on placeholder residue, `latex/main.tex` is placeholder-free in the clean retest workspace, and the rebuilt PDF succeeds at 65 pages with only 3 underfull hbox warnings.
- 2026-03-12: reran the clean C5 retest workspace end-to-end after the `front-matter-writer` / snippet sanitation / `subsection-writer` refactors. Current end-state artifacts at [workspaces/arxiv-survey/embodied-ai-smoke-20260312-c5-retest](/home/rjs/Workspace/codebase/research-units-pipeline-skills/workspaces/arxiv-survey/embodied-ai-smoke-20260312-c5-retest) are now internally consistent: `WRITER_SELFLOOP_TODO.md` PASS, `AUDIT_REPORT.md` PASS with 206 unique citations, `LATEX_BUILD_REPORT.md` SUCCESS, `main.tex` placeholder-free, and `main.pdf` rebuilt successfully.
- 2026-03-12: after the latest PASS rerun, the highest remaining quality risk is no longer chain breakage. It is reader-facing voice quality in the first impression layers: `Abstract` / `Introduction` / `Related Work` still carry planner-talk (`shared frame`, `opening contract`, repeated chapter-organization narration) and `subsection-writer` still leans on slot-shaped comparison / anchor / limitation stems plus truncated snippet clauses. These are now the top priority ahead of further helper-default cleanup.
- 2026-03-12: started `U163` directly against the latest PASS workspace: `front-matter-writer` hook assets and chapter-theme context have been rewritten to remove the worst planner-talk and repeated chapter-positioning stems; the targeted `front-matter-writer -> section-merger -> post-merge-voice-gate -> audit -> latex` rerun is again end-state PASS, but front matter still needs another pass before it reads fully natural rather than merely gate-clean.
- 2026-03-12: pushed a second `U163/U164` pass directly into `front-matter-writer` and `subsection-writer`, then rebuilt the clean retest workspace to a reader-visible preview at [workspaces/arxiv-survey/embodied-ai-smoke-20260312-c5-retest/latex/main.pdf](/home/rjs/Workspace/codebase/research-units-pipeline-skills/workspaces/arxiv-survey/embodied-ai-smoke-20260312-c5-retest/latex/main.pdf) (51 pages; LaTeX compile SUCCESS). The front matter is materially less planner-like than before, but the run is not yet ready to sign off: `WRITER_SELFLOOP_TODO.md` FAILs on thin H3s (`4.1`, `4.2`, `4.3`, `5.1`, `5.3`) and `AUDIT_REPORT.md` now FAILs with 145 unique citations plus residual paragraph fragmentation in `4.1`, `4.2`, and `5.1`.
- 2026-03-12: this preview clarifies the next real fix point. The remaining blocker is not late LaTeX or merge polish; it is `subsection-writer` still selecting some weak evidence snippets / generic cluster phrases and not consistently converting rich packs into thick, citation-dense H3 bodies. Further work should keep moving upstream rather than patching the merged draft.
- 2026-03-12: traced another reader-facing narration leak to chapter-level glue rather than subsection bodies alone. `chapter-lead-writer` was still serializing chapter-brief labels into H2 lead prose, `transition-weaver` was still emitting `Having framed ... / extends the previous subsection ...` transitions, and `section-merger` was still injecting those H3 transitions by default. The active path now uses less narrated chapter-lead defaults and no longer inserts generated H3 transitions unless a workspace explicitly opts in.
- 2026-03-12: rebuilt the same retest workspace again after those glue-layer changes. The latest preview PDF at [workspaces/arxiv-survey/embodied-ai-smoke-20260312-c5-retest/latex/main.pdf](/home/rjs/Workspace/codebase/research-units-pipeline-skills/workspaces/arxiv-survey/embodied-ai-smoke-20260312-c5-retest/latex/main.pdf) now reflects the less narrated Introduction / Related Work / H2 opening path and compiles successfully at 50 pages, but the run is still not a sign-off artifact because `subsection-writer` remains the dominant blocker for H3 naturalness, thickness, and citation coverage.
