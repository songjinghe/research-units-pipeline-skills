Data quality, scaling, and distribution shift looks settled only at a distance. Read closely, the literature supports a narrower claim: Data quality, scaling, and distribution shift methods emphasize data quality and coverage bias and scaling efficiency versus data fidelity trade-offs, but synthesis is clearest when claims are tied to explicit evaluation settings and reporting conventions. [@Fang2025Rebot; Chen2021Learning; Wang2026Rethinking; Luo2026Being]. The comparison shifts whenever scale versus data fidelity: larger embodied corpora improve coverage, but noisy supervision, heterogeneous sources, and distribution shift can erode downstream robustness..

The main split on data quality and coverage bias is between Video and temporal generation, which foreground Our approach has several advantages: 1) it enjoys the benefit of real data to minimize the sim-to-real gap; 2) it leverages the scalability of simulation; and 3) it can generalize a pretrained VLA to a target domain, while Recent representative works prioritize (2) Embodiment mixture: we find that naively pooling heterogeneous robot datasets often induces negative transfer rather than gains, underscoring the fragility of indiscriminate data scaling. [@Fang2025Rebot; Chen2021Learning; Wang2026Rethinking; Luo2026Being]. A representative empirical result is Our approach has several advantages: 1) it enjoys the benefit of real data to minimize the sim-to-real gap; 2) it leverages the scalability of simulation; and 3) it can generalize a pretrained VLA to a target domain with fully automated [@Fang2025Rebot]. Common evaluation anchors here include benchmark and protocol anchors such as VLA, VLAs, ReBot, SimplerEnv, OpenVLA, RGB, DVD, YouTube, WidowX200, ACT [@Fang2025Rebot; Chen2021Learning; Mazza2026Improving; Wang2026Rethinking].

One reason to keep the claim bounded is that However, the high cost of real-world data collection hinders further data scaling, thereby restricting the generalizability of VLAs. [@Fang2025Rebot]. On data quality and coverage bias, Video and temporal generation are associated with Our approach has several advantages: 1) it enjoys the benefit of real data to minimize the sim-to-real gap; 2) it leverages the scalability of simulation; and 3) it can generalize a pretrained VLA to a target domain; by contrast, Earlier and related works are associated with While deep learning on large and diverse datasets has shown promise as a path towards such generalization in computer vision and natural language, collecting high quality datasets of robotic interaction at scale remains [@Fang2025Rebot; Chen2021Learning; Yu2023Scaling].

A concrete data point is (2) Embodiment mixture: we find that naively pooling heterogeneous robot datasets often induces negative transfer rather than gains, underscoring the fragility of indiscriminate data scaling. [@Wang2026Rethinking]. One reason to keep the claim bounded is that a novel real-to-sim-to-real approach for scaling real robot datasets and adapting VLA models to target domains, which is the last-mile deployment challenge in robot manipulation. [@Fang2025Rebot].

The main split on data quality and coverage bias is between Recent representative works, which foreground (2) Embodiment mixture: we find that naively pooling heterogeneous robot datasets often induces negative transfer rather than gains, underscoring the fragility of indiscriminate data scaling., while Earlier and related works prioritize While deep learning on large and diverse datasets has shown promise as a path towards such generalization in computer vision and natural language, collecting high quality datasets of robotic interaction at scale remains [@Wang2026Rethinking; Luo2026Being; Chen2021Learning; Yu2023Scaling]. One anchor for this comparison is We empirically demonstrate that Being-H0.5 achieves state-of-the-art results on simulated benchmarks, such as LIBERO (98.9%) and RoboCasa (53.9%), while also exhibiting strong cross-embodiment capabilities on five robotic platforms. [@Luo2026Being].

One reason to keep the claim bounded is that Specifically, ReBot replays real-world robot trajectories in simulation to diversify manipulated objects (real-to-sim), and integrates the simulated movements with inpainted real-world background to synthesize physically realistic and [@Fang2025Rebot]. Along scaling efficiency versus data fidelity, Video and temporal generation emphasize Our approach has several advantages: 1) it enjoys the benefit of real data to minimize the sim-to-real gap; 2) it leverages the scalability of simulation; and 3) it can generalize a pretrained VLA to a target domain, whereas Recent representative works instead rely on We empirically demonstrate that Being-H0.5 achieves state-of-the-art results on simulated benchmarks, such as LIBERO (98.9%) and RoboCasa (53.9%), while also exhibiting strong cross-embodiment capabilities on five [@Fang2025Rebot; Chen2021Learning; Luo2026Being; Liu2026Rdt2].

One anchor for this comparison is To achieve this, we collected one of the largest open-source robotic datasets--over 10,000 hours of demonstrations in diverse families--using an enhanced, embodiment-agnostic Universal Manipulation Interface (UMI). [@Liu2026Rdt2]. A persistent limitation is that Our approach has several advantages: 1) it enjoys the benefit of real data to minimize the sim-to-real gap; 2) it leverages the scalability of simulation; and 3) it can generalize a pretrained VLA to a target domain with fully automated [@Fang2025Rebot].

On scaling efficiency versus data fidelity, Video and temporal generation are associated with Our approach has several advantages: 1) it enjoys the benefit of real data to minimize the sim-to-real gap; 2) it leverages the scalability of simulation; and 3) it can generalize a pretrained VLA to a target domain; by contrast, Earlier and related works are associated with While deep learning on large and diverse datasets has shown promise as a path towards such generalization in computer vision and natural language, collecting high quality datasets of robotic interaction at scale remains [@Fang2025Rebot; Chen2021Learning; Yu2023Scaling]. A concrete data point is a scalable robot augmentation pipeline, and OXE-AugE, a high-quality open-source dataset that augments OXE with 9 different robot embodiments. [@Ji2025Auge].

On scaling efficiency versus data fidelity, Recent representative works are associated with We empirically demonstrate that Being-H0.5 achieves state-of-the-art results on simulated benchmarks, such as LIBERO (98.9%) and RoboCasa (53.9%), while also exhibiting strong cross-embodiment capabilities on five; by contrast, Earlier and related works are associated with While deep learning on large and diverse datasets has shown promise as a path towards such generalization in computer vision and natural language, collecting high quality datasets of robotic interaction at scale remains [@Luo2026Being; Liu2026Rdt2; Chen2021Learning; Yu2023Scaling].
