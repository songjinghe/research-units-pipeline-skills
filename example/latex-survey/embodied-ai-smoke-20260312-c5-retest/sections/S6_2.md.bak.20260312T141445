Results in safety, reliability, and real-world deployment only stay comparable when they are read against GNN-LLM, OWMM, VLM, OWMM-VLM, SOTA, GPT-4o, HHYHRHY, OWMM-Agent, WMs, LingBot-VA. A tension around benchmark realism and task coverage and sim-to-real and deployment constraints, motivating a protocol-aware synthesis rather than per-paper summaries. [@Fang2025From; Fang2025Saga; Field2025Text2Touch; Francis2021Core].

For example, three key findings mapped to these axes: hierarchical memory systems (Capability axis) are important for long-horizon spatial tasks [@Felicia2026From]. These comparisons are usually reported against GNN-LLM, OWMM, VLM, OWMM-VLM, SOTA, GPT-4o, HHYHRHY, OWMM-Agent, WMs, LingBot-VA [@Felicia2026From; Chen2025Owmm; Baraldi2025Safety; Li2026Causal]. A persistent caveat is that we conclude by identifying six grand challenges and outlining directions for future research, including the need for unified evaluation frameworks to standardize cross-domain assessment [@Felicia2026From].

Seen through benchmark realism and task coverage, the literature separates agent-style systems from video-conditioned systems less by labels alone than by the evidence each side stresses: three key findings mapped to these axes: hierarchical memory systems (Capability axis) are important for long-horizon spatial tasks versus the approach has several advantages: it enjoys the benefit of real data to minimize the sim-to-real gap; it leverages the scalability of simulation; and it can generalize a pretrained VLA to a target domain with fully [@Felicia2026From; Fang2025Rebot]. Representative evidence comes from reports that the approach has several advantages: it enjoys the benefit of real data to minimize the sim-to-real gap; it leverages the scalability of simulation; and it can generalize a pretrained VLA to a target domain with fully [@Fang2025Rebot]. That picture has to be read cautiously because key protocol details are reported unevenly [@Chen2025Owmm].

For example, for real-world evaluation with a Franka robot, ReBot increased the success rates of Octo by 17% and OpenVLA by 20% [@Fang2025Rebot]. A persistent caveat is that a second challenge is the hallucination from domain shift [@Chen2025Owmm]. A recurrent split along sim-to-real and deployment constraints runs between agent-style systems and control and conditioning schemes: the former is usually supported by results showing that three key findings mapped to these axes: hierarchical memory systems (Capability axis) are important for long-horizon spatial tasks, whereas the latter is more often justified by evidence that where it shows significant promise in long-horizon manipulation, data efficiency in post-training, and strong generalizability to novel configurations [@Felicia2026From; Li2026Causal].

Representative evidence comes from reports that we analyze four main research targets: embodied perception, embodied interaction, embodied agent, and sim-to-real adaptation, covering state-of-the-art methods, essential paradigms, and comprehensive datasets [@Liu2024Aligning]. The comparison becomes weaker once world Models (WMs) have been introduced to provide embodied agents with the abilities to anticipate future environmental states and fill in knowledge gaps, thereby enhancing agents' ability to plan and execute actions [@Baraldi2025Safety]. Seen through sim-to-real and deployment constraints, the literature separates agent-style systems from video-conditioned systems less by labels alone than by the evidence each side stresses: three key findings mapped to these axes: hierarchical memory systems (Capability axis) are important for long-horizon spatial tasks versus the approach has several advantages: it enjoys the benefit of real data to minimize the sim-to-real gap; it leverages the scalability of simulation; and it can generalize a pretrained VLA to a target domain with fully [@Felicia2026From; Fang2025Rebot].

One concrete result is that providing insights into their strengths and limitations across different task categories [@Zhao2025Humanoid]. A persistent caveat is that yet: despite rapid advances in learning-based planning and control, reliable autonomy in real ocean environments remains fundamentally constrained by tightly coupled physical limits [@Xu2026Underwater].

A recurrent split along sim-to-real and deployment constraints runs between control and conditioning schemes and video-conditioned systems: the former is usually supported by results showing that where it shows significant promise in long-horizon manipulation, data efficiency in post-training, and strong generalizability to novel configurations, whereas the latter is more often justified by evidence that the approach has several advantages: it enjoys the benefit of real data to minimize the sim-to-real gap; it leverages the scalability of simulation; and it can generalize a pretrained VLA to a target domain with fully [@Li2026Causal; Fang2025Rebot]. Representative evidence comes from reports that using two domains--a robot arm and a humanoid--across diverse tasks, simulation data can enhance real-world task performance by an average of 38%, even with notable differences between the simulation and real-world data [@Maddukuri2025Real].

The comparison becomes weaker once hydrodynamic uncertainty, partial observability, bandwidth-limited communication, and energy scarcity are not independent challenges; they interact within the closed perception-planning-control loop and often amplify [@Xu2026Underwater]. For example, then we summarize the formal evaluation process of sim-to-real performance with accessible code or benchmarks [@Da2025Survey].

That picture has to be read cautiously because modeling these world dynamics, especially for dexterous robotics tasks, poses significant challenges due to limited data coverage and scarce action labels [@Gao2026Dreamdojo]. For example, to enhance reliability, we introduce the SANS dataset, which incorporates near-success trajectories to improve action-outcome alignment within the world model [@Liu2026World].

That picture has to be read cautiously because systematic evaluation on multiple challenging out-of-distribution (OOD) benchmarks verifies the significance of the method for simulating open-world, contact-rich tasks, paving the way for general-purpose robot world [@Gao2026Dreamdojo]. One concrete result is that all demonstrations are gathered using a consistent robotic embodiment, paired with precise subtask-level language annotations to facilitate both training and evaluation [@Jiang2025Galaxea].
