Safety, reliability, and real-world deployment only becomes comparable once its main trade-off is stated directly: A tension around benchmark realism and task coverage and sim-to-real and deployment constraints, motivating a protocol-aware synthesis rather than per-paper summaries. [@Liu2024Aligning; Zhao2025Humanoid; Zhu2025Objectvla; Fang2025Rebot]. What varies across papers is how benchmark success versus deployment realism: strong results in curated settings do not automatically translate into safe, reliable embodied behavior under real-world constraints..

Across benchmark realism and task coverage, the evidence on Agent frameworks and architectures often centers on findings that three key findings mapped to these axes: (1) hierarchical memory systems (Capability axis) are important for long-horizon spatial tasks, whereas discussion of Control and conditioning interfaces keeps returning to the fact that our model features three carefully crafted designs: (1) a shared latent space, integrating vision and action tokens, driven by a Mixture-of-Transformers (MoT) architecture, (2) a closed-loop rollout mechanism, allowing [@Felicia2026From; Li2026Causal; Brohan2022Robotics]. A concrete anchor for the comparison is that three key findings mapped to these axes: (1) hierarchical memory systems (Capability axis) are important for long-horizon spatial tasks [@Felicia2026From]. The shared evaluation frame here includes benchmark and protocol anchors such as GNN-LLM, OWMM, VLM, OWMM-VLM, SOTA, GPT-4o, HHYHRHY, OWMM-Agent, WMs, LingBot-VA [@Felicia2026From; Chen2025Owmm; Baraldi2025Safety; Li2026Causal].

The comparison stays bounded because we conclude by identifying six grand challenges and outlining directions for future research, including the need for unified evaluation frameworks to standardize cross-domain assessment [@Felicia2026From]. The comparison on benchmark realism and task coverage therefore links Agent frameworks and architectures to reports that three key findings mapped to these axes: (1) hierarchical memory systems (Capability axis) are important for long-horizon spatial tasks, while Video and temporal generation is more often discussed through results showing that our approach has several advantages: 1) it enjoys the benefit of real data to minimize the sim-to-real gap; 2) it leverages the scalability of simulation; and 3) it can generalize a pretrained VLA to a target domain [@Felicia2026From; Fang2025Rebot]. A representative result in this subsection is that our approach has several advantages: 1) it enjoys the benefit of real data to minimize the sim-to-real gap; 2) it leverages the scalability of simulation; and 3) it can generalize a pretrained VLA to a target domain [@Fang2025Rebot].

The comparison stays bounded because is the open-world mobile manipulation (OWMM) task remains a challenge due to the need for generalization to open-ended instructions and environments, as well as the systematic complexity to integrate high-level [@Chen2025Owmm]. The comparison on benchmark realism and task coverage therefore links Control and conditioning interfaces to reports that our model features three carefully crafted designs: (1) a shared latent space, integrating vision and action tokens, driven by a Mixture-of-Transformers (MoT) architecture, (2) a closed-loop rollout mechanism, allowing, while Video and temporal generation is more often discussed through results showing that our approach has several advantages: 1) it enjoys the benefit of real data to minimize the sim-to-real gap; 2) it leverages the scalability of simulation; and 3) it can generalize a pretrained VLA to a target domain [@Li2026Causal; Brohan2022Robotics; Fang2025Rebot]. One empirical anchor is that for real-world evaluation with a Franka robot, ReBot increased the success rates of Octo by 17% and OpenVLA by 20% [@Fang2025Rebot].

The comparison stays bounded because a second challenge is the hallucination from domain shift [@Chen2025Owmm]. On sim-to-real and deployment constraints, work in Agent frameworks and architectures is repeatedly justified by evidence that three key findings mapped to these axes: (1) hierarchical memory systems (Capability axis) are important for long-horizon spatial tasks; by contrast, the literature on Control and conditioning interfaces is more often framed through findings that where it shows significant promise in long-horizon manipulation, data efficiency in post-training, and strong generalizability to novel configurations [@Felicia2026From; Li2026Causal]. A representative result in this subsection is that then: we analyze four main research targets: 1) embodied perception, 2) embodied interaction, 3) embodied agent, and 4) sim-to-real adaptation, covering state-of-the-art methods, essential paradigms, and comprehensive [@Liu2024Aligning].

One limitation that changes how the result should be read is that world Models (WMs) have been introduced to provide embodied agents with the abilities to anticipate future environmental states and fill in knowledge gaps, thereby enhancing agents' ability to plan and execute actions [@Baraldi2025Safety]. The comparison on sim-to-real and deployment constraints therefore links Agent frameworks and architectures to reports that three key findings mapped to these axes: (1) hierarchical memory systems (Capability axis) are important for long-horizon spatial tasks, while Video and temporal generation is more often discussed through results showing that our approach has several advantages: 1) it enjoys the benefit of real data to minimize the sim-to-real gap; 2) it leverages the scalability of simulation; and 3) it can generalize a pretrained VLA to a target domain [@Felicia2026From; Fang2025Rebot]. A representative result in this subsection is that providing insights into their strengths and limitations across different task categories [@Zhao2025Humanoid].

The comparison stays bounded because yet: despite rapid advances in learning-based planning and control, reliable autonomy in real ocean environments remains fundamentally constrained by tightly coupled physical limits [@Xu2026Underwater]. Across sim-to-real and deployment constraints, the evidence on Control and conditioning interfaces often centers on findings that where it shows significant promise in long-horizon manipulation, data efficiency in post-training, and strong generalizability to novel configurations, whereas discussion of Video and temporal generation keeps returning to the fact that our approach has several advantages: 1) it enjoys the benefit of real data to minimize the sim-to-real gap; 2) it leverages the scalability of simulation; and 3) it can generalize a pretrained VLA to a target domain [@Li2026Causal; Fang2025Rebot]. A representative result in this subsection is that using two domains--a robot arm and a humanoid--across diverse tasks, we demonstrate that simulation data can enhance real-world task performance by an average of 38%, even with notable differences between the simulation [@Maddukuri2025Real].

One limitation that changes how the result should be read is that hydrodynamic uncertainty, partial observability, bandwidth-limited communication, and energy scarcity are not independent challenges; they interact within the closed perception-planning-control loop and often amplify [@Xu2026Underwater]. The comparison on safety and reliability failure modes therefore links Agent frameworks and architectures to reports that three key findings mapped to these axes: (1) hierarchical memory systems (Capability axis) are important for long-horizon spatial tasks, while Control and conditioning interfaces is more often discussed through results showing that our model features three carefully crafted designs: (1) a shared latent space, integrating vision and action tokens, driven by a Mixture-of-Transformers (MoT) architecture, (2) a closed-loop rollout mechanism, allowing [@Felicia2026From; Li2026Causal; Brohan2022Robotics]. One empirical anchor is that then we summarize the formal evaluation process of sim-to-real performance with accessible code or benchmarks [@Da2025Survey].

One limitation that changes how the result should be read is that modeling these world dynamics, especially for dexterous robotics tasks, poses significant challenges due to limited data coverage and scarce action labels [@Gao2026Dreamdojo]. One empirical anchor is that to enhance reliability, we introduce the SANS dataset, which incorporates near-success trajectories to improve action-outcome alignment within the world model [@Liu2026World]. One limitation that changes how the result should be read is that systematic evaluation on multiple challenging out-of-distribution (OOD) benchmarks verifies the significance of our method for simulating open-world, contact-rich tasks, paving the way for general-purpose robot world [@Gao2026Dreamdojo].

A representative result in this subsection is that all demonstrations are gathered using a consistent robotic embodiment, paired with precise subtask-level language annotations to facilitate both training and evaluation [@Jiang2025Galaxea]. A concrete anchor for the comparison is that evaluation in simulation offers a scalable complement to real world evaluations, but the visual and physical domain gap between existing simulation benchmarks and the real world has made them an unreliable signal for [@Jain2025Polaris].
