Safety, reliability, and real-world deployment only becomes comparable once its main trade-off is stated directly: a tension around benchmark realism and task coverage and sim-to-real and deployment constraints, motivating a protocol-aware synthesis rather than per-paper summaries. [@Felicia2026From; Brohan2022Robotics; Li2026Causal; Fang2025Rebot]. What varies across papers is how benchmark success versus deployment realism: strong results in curated settings do not automatically translate into safe, reliable embodied behavior under real-world constraints..

Along benchmark realism and task coverage, Agent frameworks and architectures emphasize Our analysis reveals three key findings mapped to these axes: (1) hierarchical memory systems (Capability axis) are important for long-horizon spatial tasks., whereas Control and conditioning interfaces instead rely on By transferring knowledge from large, diverse, task-agnostic datasets, modern machine learning models can solve specific downstream tasks either zero-shot or with small task-specific datasets to a high level of [@Felicia2026From; Brohan2022Robotics; Li2026Causal]. One anchor for this comparison is Our analysis reveals three key findings mapped to these axes: (1) hierarchical memory systems (Capability axis) are important for long-horizon spatial tasks. [@Felicia2026From]. Common evaluation anchors here include benchmark and protocol anchors such as GNN-LLM, OWMM, VLM, OWMM-VLM, SOTA, GPT-4o, HHYHRHY, OWMM-Agent, WMs, LingBot-VA [@Felicia2026From; Chen2025Owmm; Baraldi2025Safety; Li2026Causal].

One reason to keep the claim bounded is that We conclude by identifying six grand challenges and outlining directions for future research, including the need for unified evaluation frameworks to standardize cross-domain assessment. [@Felicia2026From]. On benchmark realism and task coverage, Agent frameworks and architectures are associated with Our analysis reveals three key findings mapped to these axes: (1) hierarchical memory systems (Capability axis) are important for long-horizon spatial tasks.; by contrast, Video and temporal generation are associated with Our approach has several advantages: 1) it enjoys the benefit of real data to minimize the sim-to-real gap; 2) it leverages the scalability of simulation; and 3) it can generalize a pretrained VLA to a target domain [@Felicia2026From; Fang2025Rebot].

A representative empirical result is Our approach has several advantages: 1) it enjoys the benefit of real data to minimize the sim-to-real gap; 2) it leverages the scalability of simulation; and 3) it can generalize a pretrained VLA to a target domain with fully automated [@Fang2025Rebot]. A persistent limitation is that However, the open-world mobile manipulation (OWMM) task remains a challenge due to the need for generalization to open-ended instructions and environments, as well as the systematic complexity to integrate high-level decision making with [@Chen2025Owmm].

On benchmark realism and task coverage, Control and conditioning interfaces are associated with By transferring knowledge from large, diverse, task-agnostic datasets, modern machine learning models can solve specific downstream tasks either zero-shot or with small task-specific datasets to a high level of; by contrast, Video and temporal generation are associated with Our approach has several advantages: 1) it enjoys the benefit of real data to minimize the sim-to-real gap; 2) it leverages the scalability of simulation; and 3) it can generalize a pretrained VLA to a target domain [@Brohan2022Robotics; Li2026Causal; Fang2025Rebot]. A concrete data point is For real-world evaluation with a Franka robot, ReBot increased the success rates of Octo by 17% and OpenVLA by 20%. [@Fang2025Rebot].

One reason to keep the claim bounded is that A second challenge is the hallucination from domain shift. [@Chen2025Owmm]. The main split on sim-to-real and deployment constraints is between Agent frameworks and architectures, which foreground Our analysis reveals three key findings mapped to these axes: (1) hierarchical memory systems (Capability axis) are important for long-horizon spatial tasks., while Control and conditioning interfaces prioritize where it shows significant promise in long-horizon manipulation, data efficiency in post-training, and strong generalizability to novel configurations. [@Felicia2026From; Li2026Causal; Brohan2022Robotics].

A representative empirical result is Then, we analyze four main research targets: 1) embodied perception, 2) embodied interaction, 3) embodied agent, and 4) sim-to-real adaptation, covering state-of-the-art methods, essential paradigms, and comprehensive datasets. [@Liu2024Aligning]. The comparison remains narrow because In this context, World Models (WMs) have been introduced to provide embodied agents with the abilities to anticipate future environmental states and fill in knowledge gaps, thereby enhancing agents' ability to plan and execute actions. [@Baraldi2025Safety].

On sim-to-real and deployment constraints, Agent frameworks and architectures are associated with Our analysis reveals three key findings mapped to these axes: (1) hierarchical memory systems (Capability axis) are important for long-horizon spatial tasks.; by contrast, Video and temporal generation are associated with Our approach has several advantages: 1) it enjoys the benefit of real data to minimize the sim-to-real gap; 2) it leverages the scalability of simulation; and 3) it can generalize a pretrained VLA to a target domain [@Felicia2026From; Fang2025Rebot]. A concrete data point is In addition, we conduct an analysis of representative policy learning methods on our dataset, providing insights into their strengths and limitations across different task categories. [@Zhao2025Humanoid].

Along sim-to-real and deployment constraints, Control and conditioning interfaces emphasize where it shows significant promise in long-horizon manipulation, data efficiency in post-training, and strong generalizability to novel configurations., whereas Video and temporal generation instead rely on Our approach has several advantages: 1) it enjoys the benefit of real data to minimize the sim-to-real gap; 2) it leverages the scalability of simulation; and 3) it can generalize a pretrained VLA to a target domain [@Li2026Causal; Brohan2022Robotics; Fang2025Rebot].
