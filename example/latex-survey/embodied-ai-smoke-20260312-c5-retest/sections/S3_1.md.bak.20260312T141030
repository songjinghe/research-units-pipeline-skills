The literature on manipulation, navigation, and task scope points to a concrete decision rather than a catalog of systems: Differences in task setting and embodiment assumptions and observation and action interface frequently imply different evaluation setups, so the key is to compare under consistent protocols where possible. [@Gundawar2025Bench; Kim2026Molmospaces; Din2025Vision; Zheng2024Survey]. The evidence becomes harder to compare once task generality versus embodiment specificity: systems that look broadly capable often rely on interfaces, sensors, or embodiments that make transfer harder than headline results suggest..

On task setting and embodiment assumptions, work in Planning and reasoning loops is repeatedly justified by evidence that extensive experiments on the AerialVLN and OpenFly benchmark validate the effectiveness of our method; by contrast, the literature on Video and temporal generation is more often framed through findings that acquiring datasets that comprehensively cover diverse tasks and environments is extremely costly and difficult to scale [@Xu2025Aerial; Kachaev2025Mind; Dai2026Conla]. A concrete anchor for the comparison is that comprehensive evaluation in simulation and the real world shows that Interleave-VLA offers two major benefits: (1) improves out-of-domain generalization to unseen objects by 2x compared to text input baselines, (2) [@Fan2025Interleave]. The relevant protocol anchors in this subsection include benchmark and protocol anchors such as VLM, MoMA, MoMaStage, VLN, UAVs, RGB, RGB-only, RGB-D, AerialVLN, OpenFly [@Li2026Momastage; Bai2025Embodied; Kachaev2025Mind; Xu2025Aerial].

One limitation that changes how the result should be read is that indoor mobile manipulation (MoMA) enables robots to translate natural language instructions into physical actions, yet long-horizon execution remains challenging due to cascading errors and limited generalization across [@Li2026Momastage]. On task setting and embodiment assumptions, work in Planning and reasoning loops is repeatedly justified by evidence that extensive experiments on the AerialVLN and OpenFly benchmark validate the effectiveness of our method; by contrast, the literature on Evaluation and benchmark-focused works is more often framed through findings that evaluations are critical to assess progress and build better policies, but evaluation in the real world, especially at a scale that would provide statistically reliable results, is costly in terms of human time and hard [@Xu2025Aerial; Kachaev2025Mind; Zhou2025Autoeval]. One empirical anchor is that including reinforcement learning, training-free optimization, learning from human videos, and world model integration; (2) synthesis of distinctiv [@Shao2025Large].

A useful boundary on this comparison is that learning-based approaches often fail to maintain logical consistency over extended horizons, while methods relying on explicit scene representations impose rigid structural assumptions that reduce adaptability in [@Li2026Momastage]. Across task setting and embodiment assumptions, the evidence on Video and temporal generation often centers on findings that acquiring datasets that comprehensively cover diverse tasks and environments is extremely costly and difficult to scale, whereas discussion of Evaluation and benchmark-focused works keeps returning to the fact that evaluations are critical to assess progress and build better policies, but evaluation in the real world, especially at a scale that would provide statistically reliable results, is costly in terms of human time and hard [@Dai2026Conla; Xu2025Aerial; Zhou2025Autoeval; Kachaev2025Mind]. A concrete anchor for the comparison is that a benchmark suite of 8 tasks in which robots interact with our diverse scenes and richly annotated objects [@Kim2026Molmospaces].

One limitation that changes how the result should be read is that recent advances in vision, language, and multimodal learning have substantially accelerated progress in robotic foundation models, with robot manipulation remaining a central and challenging problem [@Bai2025Embodied]. The comparison on observation and action interface therefore links Planning and reasoning loops to reports that extensive experiments on the AerialVLN and OpenFly benchmark validate the effectiveness of our method, while Video and temporal generation is more often discussed through results showing that acquiring datasets that comprehensively cover diverse tasks and environments is extremely costly and difficult to scale [@Xu2025Aerial; Kachaev2025Mind; Dai2026Conla]. One empirical anchor is that 26 foundational datasets, and 12 simulation platforms that collectively shape the development and evaluation of VLAs models [@Din2025Vision].

The comparison stays bounded because we identify open challenges and prospective research directions related to scalability, data efficiency, multimodal physical interaction, and safety [@Bai2025Embodied]. The comparison on observation and action interface therefore links Planning and reasoning loops to reports that extensive experiments on the AerialVLN and OpenFly benchmark validate the effectiveness of our method, while Evaluation and benchmark-focused works is more often discussed through results showing that evaluations are critical to assess progress and build better policies, but evaluation in the real world, especially at a scale that would provide statistically reliable results, is costly in terms of human time and hard [@Xu2025Aerial; Kachaev2025Mind; Zhou2025Autoeval]. One empirical anchor is that we train CrossFormer on the largest and most diverse dataset to date, 900K trajectories across 20 different robot embodiments [@Doshi2024Scaling].

One limitation that changes how the result should be read is that is a significant gap exists between benchmarks designed for high-level language instruction following, which often assume perfect low-level execution, and those for low-level robot control, which rely on simple [@Kachaev2025Mind]. The comparison on observation and action interface therefore links Video and temporal generation to reports that acquiring datasets that comprehensively cover diverse tasks and environments is extremely costly and difficult to scale, while Evaluation and benchmark-focused works is more often discussed through results showing that evaluations are critical to assess progress and build better policies, but evaluation in the real world, especially at a scale that would provide statistically reliable results, is costly in terms of human time and hard [@Dai2026Conla; Xu2025Aerial; Zhou2025Autoeval; Kachaev2025Mind]. One empirical anchor is that evaluations are critical to assess progress and build better policies, but evaluation in the real world, especially at a scale that would provide statistically reliable results, is costly in terms of human time and hard [@Zhou2025Autoeval].

A useful boundary on this comparison is that this disconnect prevents a comprehensive evaluation of integrated systems where both task planning and physical execution are critical [@Kachaev2025Mind]. Across generalization across tasks and environments, the evidence on Planning and reasoning loops often centers on findings that extensive experiments on the AerialVLN and OpenFly benchmark validate the effectiveness of our method, whereas discussion of Video and temporal generation keeps returning to the fact that acquiring datasets that comprehensively cover diverse tasks and environments is extremely costly and difficult to scale [@Xu2025Aerial; Kachaev2025Mind; Dai2026Conla]. A representative result in this subsection is that extensive experiments on the AerialVLN and OpenFly benchmark validate the effectiveness of our method [@Xu2025Aerial].

One limitation that changes how the result should be read is that kitchen-R bridges a key gap in embodied AI research, enabling more holistic and realistic benchmarking of language-guided robotic agents [@Kachaev2025Mind]. A concrete anchor for the comparison is that in a real-world multi-task training setting with five tasks, Discrete Policy achieves an average success rate that is 26\% higher than Diffusion Policy and 15\% higher than OpenVLA [@Wu2024Discrete]. One limitation that changes how the result should be read is that these requirements increase system cost and integration complexity, thus hindering practical deployment for lightweight UAVs [@Xu2025Aerial].

A representative result in this subsection is that comprehensive simulated and real-world results demonstrate that EchoVLA substantially improves overall performance, e.g., it achieves the highest success rates of 0.52 on manipulation and navigation tasks and 0.31 on [@Lin2025Echovla]. One empirical anchor is that (1) task diversity proves more critical than per-task demonstration quantity, benefiting transfer from diverse pre-training tasks to novel downstream scenarios; (2) multi-embodiment pre-training data is o [@Shi2025Diversity].
